# 论文对齐与实现选择

[English](ALIGNMENT.md) | 简体中文

依据：[OmegaPRM arXiv v2，2024-12-11](https://arxiv.org/html/2406.06592v2)。
以下描述的是本项目的实现合同，不代表已经复现论文的数据或实验数值。

## 已据论文校准的主要设置

| 项目 | 本版默认 | 对照位置 |
| --- | --- | --- |
| 题目筛选 | 32条，保留同时有对有错的题 | Appendix A |
| MC估计 | 每前缀8条，正确数/8 | §3.2、§4 |
| 搜索上限 | 100次/题，包括选择根节点 | §4 |
| Q | `0.5**(1-MC) * 0.9**(rollout_tokens/500)` | §3.3、§4 |
| U | `0.125 * sqrt(sum(visits)) / (1+visits)` | §3.3、§4 |
| 二分 | 正MC向右；零MC向左 | §3.2 |
| 片段阈值 | 平均解答token数/16 | §4.2 |
| 训练目标 | 单步边的soft MC；同时保存hard label | §3.4、§4.3 |

## 不完全由论文确定的细节：本项目如何处理

这些是具体、可复现的实现选择。后续如获得作者代码/澄清，应按证据更新，不能宣称已与作者逐行等价。

**长度平均的统计总体。** 本版使用本次保留题目在32次筛题阶段的完整解答。
两种结果都参加平均，统一使用生成模型tokenizer，排除不可见终止token。
平均值对整个run固定；不按每个被选择的rollout重新估计。
支持外部指定平均长度，方便多个baseline共用校准。

**切分位置与不可分片段。** 以fast tokenizer的offset映射选取最接近token中点的原文字符边界。
保留原文，不decode半个Unicode字符，不按行或单词划分。
当剩余区间token数严格小于阈值时停止；只有一个token或不存在合法内部字符边界时也停止。

**单步边。** 树保存source到各正切分点、再到最终零边界的路径。两个相邻节点的原文差值是action。
action满足同一个长度停止条件才导出为单步样本；长action作为多步边留在审计数据。
没有为未测量的中间前缀补造MC，也没有把长action的标签复制给其中每个token。

**候选池及维护。** 候选限于`0<MC<1`状态下的未访问错误rollout；正确rollout仍参与MC分母。
状态和续写会缓存。每个新采样位置增加一组k条续写并计算MC；已缓存前缀直接复用。
选择后只增加该状态的访问次数，不递归增加祖先访问次数。分数平局按插入顺序打破。
原文描述候选池时使用“all rollouts”，但错误定位及优先级说明针对错误解答；本版明确采用后者。

**32次筛题与8次根估计。** 本版采用两个独立阶段，不把32条筛题数据直接作为MC=8的根节点。
若8次根采样全对/全错，就没有候选；记录`root_no_candidates`并结束该题，不额外重试。
后续二分时遇到全对/全错则用来决定搜索方向，不会丢弃该题。

**终止节点。** 已验证答错的完整解答作为吸收终止状态，其价值为0，不再继续生成。
保留`mc_source=terminal_answer`和真实的0条额外rollout计数。
这是对终止条件的显式约定；非终止前缀的标签均来自实际采样。

**缓存与去重。** 相同原文前缀合并，因此审计对象是概念搜索树的去重前缀DAG。
同一对父子边只导出一次；不会把重复选择同一文本造成的重复标注作为新样本。

**验证失败。** 完整但无法识别为正确的最终答案默认算0，MC分母不变，同时保存失败原因。
超时/验证器执行异常、截断、上下文超限默认使run报错。政策都记录在manifest；它们不是论文明确给出的规则。

## 有意保留的实验差异

- 默认Qwen数学7B指令模型用于小规模生成，**不等同于论文的Gemini或Gemma2模型及提示设置**。
- Math-Verify替代了未从论文中恢复的原答案判定实现；需要人工检查本数据集上的判定质量。
- 温度0.8、top_p0.95、2048续写上限、具体提示词与seed是本项目设置，不标成论文超参数。
- 输入是使用者提供的训练子集，不自动重建原训练/测试划分。
- 小规模运行导出全部唯一单步边；不额外平衡正负例，也不强行生成指定数量的数据。
- `--max-calls`、不完整输出算错等非默认开关一旦启用，需作为预算/失败政策的差异报告。

## 用作baseline时需固定并报告

生成模型与revision；训练子集和排除的测试题；筛题后的题目ID；校准平均长度；
提示模板与tokenizer；温度/top_p/最大续写长度；seed；验证器；失败政策；
实际筛题/搜索成本；正负样本数。`manifest`、`calibration`、`summary`与逐题审计文件提供这些信息的大部分。

baseline结论应限于当前生成器和数据设置。即使所有软件测试通过，
有限采样和非单调的自我纠错仍会导致过程标签噪声；二分边界不是形式化的逻辑错误证明。

其他实现参考：
[vLLM文档](https://docs.vllm.ai/en/latest/)、
[Math-Verify](https://github.com/huggingface/Math-Verify)、
[默认Qwen模型上下文配置](https://huggingface.co/Qwen/Qwen2.5-Math-7B-Instruct/blob/main/config.json)。
